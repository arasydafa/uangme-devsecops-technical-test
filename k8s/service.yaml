apiVersion: v1
kind: Service
metadata:
  name: helloworld-service
  namespace: default
  labels:
    app: helloworld
spec:
  type: NodePort
  selector:
    app: helloworld
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
